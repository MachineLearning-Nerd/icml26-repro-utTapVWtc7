# Claim 4 four-route assessment

Confidence remained **LOW** after three materially different routes, so the
mandatory fourth route was dedicated to falsification. None of the routes
verified or falsified the exact five-space, full-target-domain contract.

The direct DARTS calibration is intentionally separated from paper evidence:
its 16 released rows and 128 raw draws validate the code path and establish a
CPU runtime, but the paper requires ranking every remaining target-space row
after excluding the exact few-shot training rows. Those identities are not
released. The missing ENAS and NASNet target-specific checkpoints prevent
complete five-space evaluation.

The fourth route found no assumption-satisfying counterexample. A wrong model,
an arbitrary subset, an unknown train/test overlap, or an inaccessible
checkpoint cannot falsify the reported Table 4 result. Final verdict:
`BLOCKED`.
